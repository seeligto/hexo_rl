"""CONFRES config-resolution package — one resolution authority per regime knob.

See docs/designs/confres_design.md. Consumers read resolved values; no consumer re-derives
a regime knob from raw config outside ``hexo_rl.config.resolve``.
"""
