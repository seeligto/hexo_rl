# fast_debug.yaml — quick smoke-test overrides
# Applied on top of base configs (model.yaml + training.yaml + selfplay.yaml).
# NEVER override architecture here (res_blocks, filters, board_size).

training:
  max_steps: 100
  batch_size: 32

selfplay:
  n_workers: 2

eval_pipeline:
  eval_interval: 9999999  # disable eval in smoke tests
