# model.yaml — model architecture hyperparameters
# Single source of truth for all architecture dimensions.
# Never override these in other config files.

board_size: 19
# 8 = cur ply-0..3 + opp ply-0..3 (HEXB v6 wire format).
# Chain-length planes (Q13) are output-only auxiliaries; not in input.
in_channels: 8
res_blocks: 12
filters: 128
se_reduction_ratio: 4

# §166 v8 encoding gate. Default "v6" preserves pre-§166 behavior byte-exact.
# Set to "v8" to route the Path β pipeline (25×25 bbox, 11 planes, R=8, no
# K-aggregation, no pass slot). Phase A wires the v8 plumbing only; v8 model
# architecture, corpus regen, and self-play encoding-awareness land in
# Phases B–D. See docs/designs/encoding_v8_contract.md.
encoding:
  version: "v6"
