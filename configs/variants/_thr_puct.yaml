# throughput-test override (bench-only) — PUCT arm config.
encoding: v6_live2_ls
in_channels: 4
selfplay:
  gumbel_mcts: false
  inference_max_wait_ms: 16.0
  inference_batch_size: 128
  leaf_batch_size: 8
